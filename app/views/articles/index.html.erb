
<div class="container">
<h1>Articles</h1>
<div class="alert alert-success" role="alert">Check back often for updates!!</div>

Our blog has <%= Article.public_count %> articles and counting!

</div>
<br>
<div class="container">
<ul class="list-group">
    <div class="row row-cols-3">
    <% @articles.each do |article| %>
        <% unless article.archived? %>
        <div class="col">
            <li class="list-group-item">
                <%= link_to article.title, article %>
                <p><%= article.body %></p>
            </li>
            </div>
        <% end %>
    <% end %>
    </div>
</ul>
</div>